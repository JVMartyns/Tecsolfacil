  Create a CSV file
  
  def to_csv(data) do # data is [ [...] ]
    col_sep = ","
    row_sep = "\n"

    csv =
      for row <- data, into: "" do
        Enum.join(row, col_sep) <> row_sep
      end

    File.write(csv, "test.csv")
  end
